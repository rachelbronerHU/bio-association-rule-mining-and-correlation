Each notebook here is a rule-by-rule diagnostic companion to the notebook named in its title. Run its source notebook first, then choose `show_selected(index, stage=...)`. The plots compare a rule-bearing FOV with an eligible no-rule FOV and reconstruct the binary CN patch logic. Center variants are shown separately here to explain the pooled cell-type rule; their stored mining FDRs are not averaged.
